# Raw gaussian-splat training artifacts — never commit (huge, redundant with .sog)
raw/
*.ply

# OS noise
.DS_Store

# Node
node_modules/

# Python bytecode and the pose cache tools/sfm_vis.py writes
__pycache__/
*.pyc
*.npz
